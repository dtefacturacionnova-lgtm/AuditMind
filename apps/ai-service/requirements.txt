# ─── Core ──────────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
python-multipart>=0.0.12
httpx>=0.27.0

# ─── Pydantic ──────────────────────────────────────────────────────────────────
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ─── LLM — Anthropic (Claude) ──────────────────────────────────────────────────
anthropic>=0.36.0

# ─── Embeddings — Gemini (gemini-embedding-001, 3072 dims, free) ───────────────
google-genai>=1.0.0

# ─── Auth ──────────────────────────────────────────────────────────────────────
PyJWT>=2.9.0

# ─── Database ──────────────────────────────────────────────────────────────────
asyncpg>=0.30.0
pgvector>=0.3.2
psycopg2-binary>=2.9.9

# ─── Data Science — CAATs ──────────────────────────────────────────────────────
pandas>=2.2.0
numpy>=2.0.0
scipy>=1.14.0
statsmodels>=0.14.0
scikit-learn>=1.5.0
openpyxl>=3.1.0
jsonschema>=4.23.0

# ─── Document Processing ───────────────────────────────────────────────────────
pdfplumber>=0.11.0
Pillow>=10.4.0

# ─── Evidencia de Campo — transcripción de audio autoalojada (EVD-03) ──────────
# Trae ctranslate2 + av (PyAV empaqueta ffmpeg) — no requiere ffmpeg de sistema.
faster-whisper>=1.0.0

# ─── Evidencia de Campo — diarización de hablantes (EVD-12, Fase 2) ────────────
# Trae torch + torchaudio (nuevo en este proyecto, ~500MB-1GB). Modelo "gated" —
# requiere HUGGINGFACE_TOKEN con licencia aceptada (ver diarization_service.py).
pyannote-audio>=3.1.0
